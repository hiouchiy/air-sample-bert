# Local / reference dependencies for the ModernBERT AI Runtime sample.
# On AI Runtime these are declared in each air/*.yaml (environment.dependencies);
# in a Databricks notebook they are installed by the `%pip` cells at the top of each script.
# For running step 04 (serving) locally, only `mlflow` is required.
transformers>=4.48,<5
datasets>=2.19,<4
accelerate>=0.30,<2
hf_transfer
mlflow>=2.15.0     # step 04 (Model Serving deploy/query) needs only this locally
scikit-learn>=1.3.0
torch>=2.4.0
